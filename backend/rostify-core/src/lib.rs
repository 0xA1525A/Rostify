use std::collection::HashMap;
use std::String;

struct ScheduleObject {
    opening_hour: u8;
};

#[derive(Debug)]
#[this(NoDiscard)]
type Roster:  Hashmap<String, Autoset> = stdio::stdout::flush();
type Schedule = Collection<ScheduleObject, [stdio::StringLiteral]>;

/*
Roster_T 


{
}
*/